import 'package:flutter/material.dart';

/**
 * App Colors Design Tokens
 * DHOLERA REAL ESTATE
 */
class AppColors {
  // Brand Palette
  static const Color primary = Color(0xFF0A2540);        // Deep Navy Blue
  static const Color primaryAccent = Color(0xFF00D4B2);  // Vibrant Teal Accent
  static const Color primaryDark = Color(0xFF06182B);    // Dark Navy
  
  // Secondary & Text Colors
  static const Color secondary = Color(0xFF1A1F36);      // Dark Slate Heading Text
  static const Color textPrimary = Color(0xFF1A1F36);
  static const Color textSecondary = Color(0xFF697386);  // Muted Subtitle Text
  static const Color textLight = Color(0xFFA5ACB8);

  // Background & Surface
  static const Color background = Color(0xFFF8F9FC);     // Light Cool Grey Page Background
  static const Color surface = Color(0xFFFFFFFF);        // White Card Background
  static const Color border = Color(0xFFE3E8EE);         // Input Outline / Divider

  // Status Indicators
  static const Color success = Color(0xFF0E9F6E);        // Active Badge / Success Toast
  static const Color error = Color(0xFFF05252);          // Inactive Badge / Danger Actions
  static const Color warning = Color(0xFFFF9900);        // Alert / Warning
  static const Color info = Color(0xFF3F83F8);           // Info Chip
}
